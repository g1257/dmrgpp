# Every plot and intermediate data file in this directory is generated by
# the Python tooling here (see README.md's "Regenerating plots" section and
# ./regenerate_plots.sh) -- none of it belongs in version control. Binary
# blobs (plots, npz arrays) bit-rot in git history and can't be usefully
# diffed/reviewed; the source (scripts + parameters) is what's checked in.
*.png
*.npz
*.provenance.txt
__pycache__/
.DS_Store
cpp_docc_energy/
arxiv_figures/
report.pdf
report.aux
report.log
report.out
report.toc
report-summary.pdf
report-summary.aux
report-summary.log
report-summary.out
report-summary.toc
*.fls
*.fdb_latexmk
*.synctex.gz

# Generated ASCII Green's-function dumps from the atomic-limit reference
# tooling -- same "generated, not source" rule as the plots/npz above.
gbek-atomic-limit-exact-lesser*
gbek-eq71-seed-lesser
